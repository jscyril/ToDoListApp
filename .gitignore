# Built output
*.iml
.gradle
/build
/captures
.externalNativeBuild
.cxx

# Local environment files (contains SDK path — machine-specific, not portable)
local.properties
/local.properties

# IDE files — IntelliJ / Android Studio
/.idea/
.idea/

# Keystore files — NEVER commit signing keys to version control
*.jks
*.keystore
*.pfx

# OS junk
.DS_Store
Thumbs.db

# Kotlin / Java
*.class
*.log

# Gradle wrapper jar (optional — include if you want reproducible builds offline)
# gradle/wrapper/gradle-wrapper.jar
